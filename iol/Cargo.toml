[package]
name = "iol"
version = "0.0.0"
edition = "2021"

[features]
device = []
master = []
iols = []

[dependencies]
log = "0.4.27"

# [dev-dependencies]
# tokio = { version = "=1.45.1", features = ["macros", "rt-multi-thread"] }
# mockall = "=0.13.1"
