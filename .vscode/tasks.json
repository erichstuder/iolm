{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "firmware_build",
            "type": "shell",
            "command": "cd examples/stm32f446re && cargo build",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$rustc"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}
